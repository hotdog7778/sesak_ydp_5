<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>폼</title>
  </head>
  <body>
    <h1><%= title %></h1>
    <h2>GET 요청</h2>
    <form action="/getForm" method="get">
      <input
        type="text"
        name="id"
        placeholder="ID는 최소 3글자"
        required
        minlength="3"
      />
      <input
        type="password"
        name="pw"
        placeholder="PW는 최소 4글자"
        required
        minlength="4"
      />
      <input type="email" name="email" placeholder="EMAIL은 필수값" />
      <input type="number" name="grade" placeholder="GRADE" />
      <button>제출</button>
    </form>
    <h2>POST 요청</h2>
    <form action="/postForm" method="post">
      <input
        type="text"
        name="id"
        placeholder="ID"
        pattern="^([a-zA-Z0-9가-힣]){4,}$"
        title="아이디는 대소문자, 한글, 숫자로 구성된 4자 이상의 문자여야 합니다."
        required
      />
      <input
        type="password"
        name="pw"
        placeholder="PW"
        pattern="^([a-zA-Z0-9]){6,12}$"
        title="비밀번호는 영문,숫자로 이루어져야 하며, 범위는 6자 ~ 12자"
        required
      />
      <button>제출</button>
    </form>
  </body>
</html>
